mixin banner-top(data)
    section.section-banner-top
        .banner-top
            .banner-top__title
                h1= data.title
            .banner-top__image
                +picture(data.image)